import { FilterNomJvPipe } from './filter-nom-jv.pipe';

describe('FilterNomJvPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterNomJvPipe();
    expect(pipe).toBeTruthy();
  });
});
